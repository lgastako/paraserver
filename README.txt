paraserver

Introduction
============

Pre-requisites
==============

You need node.js and npm installed.  From there you can install the rest
with npm (drop the -g if you want them installed locally instead of
globally):

    npm install express -g
    npm install jsdom -g
    npm install request -g
    # dependencies
    npm install -d -g

